const fs=require('fs');const path=require('path');let ts;try{ts=require('typescript')}catch{ts=require('/opt/nvm/versions/node/v22.16.0/lib/node_modules/typescript')}
const root=path.resolve(__dirname,'..');let files=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(['node_modules','dist','.next'].includes(e.name))continue;const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(/\.tsx?$/.test(e.name)&&!e.name.endsWith('.d.ts'))files.push(p)}}walk(root);
let failed=0;for(const f of files){const src=fs.readFileSync(f,'utf8');const result=ts.transpileModule(src,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext,jsx:ts.JsxEmit.Preserve},reportDiagnostics:true,fileName:f});for(const d of result.diagnostics||[]){if(d.category===ts.DiagnosticCategory.Error){failed++;console.error(f,ts.flattenDiagnosticMessageText(d.messageText,' '));}}}
if(failed)process.exit(1);console.log(`TypeScript syntax check passed for ${files.length} files`);
